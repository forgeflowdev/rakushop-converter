window.ForgeFlowUI=(()=>{const $=s=>document.querySelector(s);const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));function setProgress(p,l){$("#progressWrap").classList.remove("hidden");$("#progressBar").style.width=p+"%";$("#progressValue").textContent=p+"%";$("#progressLabel").textContent=l;if(p>=100)setTimeout(()=>$("#progressWrap").classList.add("hidden"),500)}function buildMapping(headers,mapping,onChange){const box=$("#mapping");box.innerHTML="";for(const[key,label]of ForgeFlowConverter.fields){const d=document.createElement("div");d.innerHTML=`<label>${esc(label)}<select data-key="${key}"><option value="">未使用</option>${headers.map(h=>`<option value="${esc(h)}"${mapping[key]===h?" selected":""}>${esc(h)}</option>`).join("")}</select></label>`;box.appendChild(d)}box.querySelectorAll("select").forEach(s=>s.addEventListener("change",()=>onChange(s.dataset.key,s.value)))}function renderIssues(issues){$("#issuesCard").classList.remove("hidden");$("#issues").innerHTML=issues.length?issues.map(x=>`<div class="issue ${x[0]}">${esc(x[1])}</div>`).join(""):`<div class="notice ok">大きな問題は見つかりませんでした。</div>`}function renderPreview(rows){const hs=Object.keys(rows[0]||{});$("#preview").innerHTML=`<thead><tr>${hs.map(h=>`<th>${esc(h)}</th>`).join("")}</tr></thead><tbody>${rows.slice(0,30).map(r=>`<tr>${hs.map(h=>`<td>${esc(r[h])}</td>`).join("")}</tr>`).join("")}</tbody>`;$("#previewCard").classList.remove("hidden")}function download(name,content){const blob=new Blob(["\uFEFF",content],{type:"text/csv;charset=utf-8"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}return{$,setProgress,buildMapping,renderIssues,renderPreview,download}})();